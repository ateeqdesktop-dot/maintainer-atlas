# Contributing

Maintainer Atlas is intentionally dependency-light and passive. Create a focused fixture for every rule change, run `python3 -m pytest -q`, and explain any policy or security impact in the pull request. Do not add code that executes repository content, follows links, or uploads source.
